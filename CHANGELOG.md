# DLsite 搜刮器 —— 版本变更记录

## 一、这个插件是干什么的

给 **PotatoVN** 用的**搜刮器插件**（实现 `IPlugin` + `IParserProvider`，市场类型 `types=4`）。
把 **DLsite** 上某个作品的信息抓回来，写进 PotatoVN 的游戏记录。

| 抓什么 | 来源 |
| --- | --- |
| 标题（**日文原名**） | JSON `work_name` / 作品页 `og:title` |
| 社团（开发商） | JSON `maker_name` |
| 发售日 | HTML 规格表 `販売日`（旧表头 `発売日`） |
| 标签 | HTML `ジャンル`（空格分隔） |
| 封面 | JSON `image_main.url`（协议相对，需绝对化） |
| 简介 | JSON `intro_s`（`intro` 恒为 null） |
| staff（声优 / 原画 / 剧本 / 音乐） | JSON `creaters.<role>_by[]` + HTML |
| 头图 | 未实现（JSON 与页面里都没有对应字段） |

覆盖分区：`maniax` / `pro` / `soft` / `girls` / `comic` / `home`。
数据来源是 **JSON 接口 + 作品页 HTML 双路抓取再合并**（JSON 字段名与直觉不符，HTML 补它缺的）。

**三种用法**

1. 设为「设置 -> 下载行为 -> 信息源」的默认源 -> 拖入新游戏时第一次自动搜刮就走它；
2. 游戏详情页把信息源选成本插件 -> 点「从信息源获取」；
3. 直接把作品号（`RJxxxxxx` / `VJxxxxxx` / `BJxxxxxx`）填进本插件的 id 栏。

> 注意：拖入新游戏后弹的「确认游戏信息」弹窗里的**源下拉框，上游只列内置源**，
> 插件默认不会出现在那里；可以装上配套的「新增源插件」，或去游戏详情页换源。

---

## 二、每一版改了什么

### 1.0 -> 3.0：解析修复 + 自动搜索首次可用

**解析**

- `FetchWorkAsync` 原本「JSON 一有数据就 return」，HTML 永不执行 -> 封面 / 简介 / staff 全空；
  改为两个来源都抓再 `Merge`
- JSON 字段名大面积对不上：正文在 `intro_s`（`intro` 恒为 null）、封面在 `image_main.url`、
  staff 在 `creaters[role_by][]`（顶层 `voice_by` 等恒为 null）-> 按实测键名重映射
- 规格表表头 `発売日` 已改名 `販売日` -> 两个都认
- `ジャンル` 是**空格**分隔且标签自带斜杠（`学校/学園`）；staff 用「空格 / 空格」分隔
  -> 拆成 `SplitGenre`（只按空白）与 `SplitStaff`（按 `/` 但跳过括号内）
- JSON 路径没做 URL 绝对化（封面是协议相对的 `//img.dlsite.jp/...`）-> `Merge` 后统一 `NormalizeUrls`
- 标题常带促销尾巴（`【クーポン利用で20%OFF！】（...まで）`）-> `CleanTitle` 只删这一类

**自动搜索**

- 搜索词从显示名改为 **`OriginalName`（日文原名）优先**，再退显示名 / 中文名
- **逐分区搜索**：DLsite 的搜索按站点分区隔离，`/all/` 不存在

**打包**

- 黑名单补上三个用不到的 WindowsAppSDK 大件，包从 663 KB 降到约 432 KB
- AngleSharp 1.3.0 -> 1.8.2（1.3.0 有 mXSS 类已知漏洞）

### 3.1：长标题与同系列误选

- **搜索词派生截短变体**：DLsite 的 `fsr` 搜索要求**所有分词都命中**，
  长标题一旦是「标题 + 空格 + ～副标题～」就整个 0 结果。做法：每个名字派生
  「完整 -> 截到第一个空白 -> 去尾部符号 -> 截到第一个 `～` / `（` / `【`」，
  去重后最多 4 个；且**先把 maniax 跑完所有变体**再试其他分区（连续请求会 403，请求数要省）
- **打分改用完整原名**（`FullNameForScoring`），截短词只给 `PrefixFloorScore = 0.80` 兜底，
  避免同系列续作被选中

### 3.2：移除第三方反查桥

- 插件运行时**不再访问任何第三方站点**，也不再需要外部 Cookie 文件；只跟 `dlsite.com` 说话
- 搜不到时的日志从「改用第三方反查」改成**明确指引**：先用内置源刮一次拿到日文原名，或直接填作品号

### 3.3

- 发布构建加 `PathMap`，dll / pdb 里不写入开发者本地源码路径
- 文档与注释里不再出现本机路径、游戏库盘符等信息

---

## 三、自动搜索的边界

- 库里 `OriginalName` 有值（日文原名）-> **能自动搜到**（长标题会自动截短再试）
- 只有中文名（刚拖进来的游戏）-> 搜不到。DLsite 索引里没有中文标题，这一点没有绕法：
  先用内置源（Bangumi 等）刮一次，宿主会把日文原名写进 `OriginalName`；或直接填作品号
- 站内确实没有的作品 -> 只能手填 id
- 搜不到时**不静默**：日志写明「DLsite 各分区都没搜到（试过：...）」+ 自查建议
- 相似度低于 0.75 一律放弃，不会把错误的作品写进库

同时确认可用的站点能力：JSON 接口、作品页、`age_check_done=1` 年龄 Cookie、
`og:title/image/description`、`th/td` 规格表、按名字搜索的 `fsr/=/keyword/` 兜底页。

**仍未实现**：头图（`IGalHeadersParser`）—— JSON 与页面里都没找到横版头图字段，
`HeaderUrl` 保持为空，不拿封面冒充。

---

## 四、排障

- 设置环境变量 `DLsiteParser_DumpRaw=1` 会把原始响应写到插件目录，便于站点改版时定位
- 自动搜索失败时先看日志：它会列出试过的关键词与分区